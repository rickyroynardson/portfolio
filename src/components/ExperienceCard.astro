---
const { timeline, title, company, type } = Astro.props;
---

<div
  class="border-l-2 border-stone-600 border-opacity-30 px-3 sm:px-5 py-2 sm:py-3"
>
  <p class="text-sm md:text-base text-stone-800">{timeline}</p>
  <p class="text-stone-950 font-medium">{title}</p>
  <p class="text-sm md:text-base text-stone-800">{company} - {type}</p>
</div>
